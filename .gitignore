# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Test / lint caches
.pytest_cache/
.ruff_cache/

# Local Claude settings (per-machine permissions)
.claude/

# Local result backups (the canonical run lives in results/tables/)
results/tables_backup_*/

# Logs / scratch
*.log

# Scratch dir created by a shell that failed to expand %TEMP% / $env:TEMP
# (the multiseed smoke run landed here instead of the system temp dir).
SERSBEDRHA~1APPDATAocaltemp/
