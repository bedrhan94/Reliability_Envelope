# Reproducibility lock for the reliability-envelope runs (captured 2026-07-02).
# Exact package versions used to produce results/tables. The `>=` bounds in
# pyproject.toml define compatibility; this file pins what actually ran, so a
# reviewer can reproduce the numbers. python==3.10.0
numpy==2.2.6
pandas==2.3.3
scikit-learn==1.7.2
scipy==1.15.3
pyyaml==6.0.3

# GBDT reference backends
xgboost==3.2.0
catboost==1.2.10
lightgbm==4.7.0  # strong-baselines arm (Table XVIII); current-machine version

# ICL foundation-model backends. TabPFN is run from LOCAL pinned weights (tabpfn 8.2.0)
# for the multiseed benchmark -- this is version-pinnable, unlike the cloud client, which
# is why the reproducibility limitation on the cloud backend is retired. tabpfn-client is
# kept for the seed-42 6-model cloud comparison.
tabpfn==8.2.0
tabpfn-client==0.3.2
tabicl==2.1.1
torch==2.7.1+cu128

# figures + dev tooling
matplotlib==3.10.8
pytest==9.1.1
ruff==0.15.20
